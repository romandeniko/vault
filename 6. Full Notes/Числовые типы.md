---
created: 2026-07-10T15:39:00
status:
links:
  - "[[Типы данных Python]]"
  - "[[Python]]"
  - "[[Переменные Python]]"
---
### Целые числа
```python
# Положительные и отрицательные целые числа
positive = 42
negative = -73
zero = 0

big_number = 1234567890123456789012345678901234567890

# Подчёркивание-разделитель для удобства чтения больших чисел
million = 1_000_000  # То же самое, что 1000000
```

### Числа с плавающей точкой
```python
price = 19.99
pi_approx = 3.14159
negative_float = -0.5
```

Для вычислений с критической точностью использовать `decimal`.

Для сравнения  с допуском использовать `math.isclose(a, b)`.

```python
import math
print(math.isclose(0.1 + 0.2, 0.3))
```

### Арифметические операции
```python
a = 10
b = 3

# Сложение, вычитание, умножение
a + b   # 13
a - b   # 7
a * b   # 30

# Деление — всегда возвращает float
a / b   # 3.3333333333333335

# Целочисленное деление — возвращает int (отбрасывает дробь)
a // b  # 3

# Остаток от деления
a % b   # 1

# Возведение в степень
a ** b  # 1000
```

###### Смешанные типы
```python
5 + 3.14    # 8.14 (float)
10 * 2.0    # 20.0 (float)
4 ** 0.5    # 2.0  (float)
```

### Округление
```python
# Округление до ближайшего целого
round(3.14159)      # 3

# Округление до указанного количества знаков после запятой
round(3.14159, 2)   # 3.14

import math

# Округление вниз (к меньшему целому)
math.floor(3.99)    # 3

# Округление вверх (к большему целому)
math.ceil(3.01)     # 4

# Отбрасывание дробной части
math.trunc(3.99)    # 3
```

Python использует банковское округление, т.е. округляет до ближайшего четного числа.
Для школьного округления использовать `decimal`.

### Математические функции
```python
import math

math.sqrt(16)   # 4.0
math.pi         # 3.141592653589793
math.e          # 2.718281828459045
```
https://docs.python.org/3/library/math.html

### Встроенные функции для чисел
```python
# Абсолютное значение
abs(-10)              # 10

# Максимум и минимум
max(1, 5, 3, 9, 2)    # 9
min(1, 5, 3, 9, 2)    # 1

# Сумма последовательности
sum([1, 2, 3, 4, 5])  # 15

# Преобразование других типов в числовые
int("42")             # 42
float("3.14")         # 3.14
```